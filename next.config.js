/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  images: {
    domains: ["res.cloudinary.com", "wordpress.stcsystems.ro"],
  },
};

module.exports = nextConfig;
